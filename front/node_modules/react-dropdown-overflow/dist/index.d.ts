import * as React from "react";
import "./dropdown.scss";
export declare type Positioning = "center" | "left" | "right";
export declare type Mode = "hover" | "click";
export declare type Props = {
    children: React.ReactNode;
    className?: string;
    dropdown: React.ReactNode;
    dropdownWrapperId: string;
    hasClickOutsideListener?: boolean;
    isOpen?: boolean;
    mode: Mode;
    positioning: Positioning;
    wrapperId?: string;
    zIndex: "auto" | number;
};
declare function Dropdown({ children, className, dropdown, dropdownWrapperId, hasClickOutsideListener, isOpen, mode, positioning, wrapperId, zIndex, ...props }: Props): JSX.Element;
export default Dropdown;
